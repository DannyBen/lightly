language: ruby

env:
  global:
  - CC_TEST_REPORTER_ID=6bfff0c50677d32902122996ef3465c050080e43beacbe24bfdfe3e27ebf082b

rvm:
  - "2.0.0"
  - "2.5.0"

notifications:
  email: false

before_install:
- gem install bundler

before_script:
- curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
- chmod +x ./cc-test-reporter
- ./cc-test-reporter before-build

script:
- bundle exec rspec

after_script: 
- ./cc-test-reporter after-build --exit-code $TRAVIS_TEST_RESULT